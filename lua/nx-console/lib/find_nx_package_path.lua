local Path = require("plenary.path")

local M = {}

-- M.find_nx_package_path = function(workspace_path, file_path)
--   local build_path = function(base)
--     return Path:new(base, file_path)
--   end
--
--   local nx_workspace_dap_path = nil
-- end

return M
